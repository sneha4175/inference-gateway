"""AI Inference Gateway — a FastAPI service that proxies LLM chat/completion
requests with rate limiting, caching, cost tracking and provider fallback,
plus a retrieval-augmented-generation (RAG) route."""

__version__ = "0.1.0"
