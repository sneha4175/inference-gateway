# Runtime + test dependencies. Pinned to compatible minor ranges so the build
# is reproducible without freezing us to a single patch release.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
httpx>=0.27,<1.0        # real provider transport + Starlette TestClient
numpy>=1.26,<3.0        # vector store (cosine similarity)
prometheus-client>=0.20,<1.0   # /metrics Prometheus text exposition

# --- test only ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
