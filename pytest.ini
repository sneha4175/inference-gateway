[pytest]
# asyncio_mode = auto lets us write `async def test_...` without decorating each
# one. The gateway/provider interface is async, so several tests are too.
asyncio_mode = auto
testpaths = tests
addopts = -q
